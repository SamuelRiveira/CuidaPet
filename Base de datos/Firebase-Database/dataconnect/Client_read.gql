
# This is a file for you to write an un-named query.
# Only one un-named query is allowed per file.
query {
  clients{
    user {
      name
      email
      password
      role
    }
    address
    phone
  }
}